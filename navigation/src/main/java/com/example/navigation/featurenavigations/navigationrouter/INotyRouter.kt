package com.example.navigation.featurenavigations.navigationrouter

import android.content.Context

interface INotyRouter {

    fun toDiaryCreation(context: Context)

    fun toDiaryList(context: Context)
}